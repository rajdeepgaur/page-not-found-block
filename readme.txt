# Page Not Found Block – Gutenberg Custom Block

A custom Gutenberg block that helps you gracefully handle 404 or "Page Not Found" scenarios within WordPress. This block allows content creators to dynamically display a friendly message when the requested page or URL does not exist.

## 🧩 Features

- Displays the last segment of the current URL
- Optionally prepend a prefix and/or append a suffix
- Toggle visibility of prefix and suffix independently
- Lightweight and compatible with full site editing (FSE)
- Supports custom block styling via block editor

## 🚀 Use Case

Ideal for creating user-friendly "Page Not Found" templates in block themes. You can insert this block in a 404 template to give a more informative and customized message like:

== Installation ==

1. Upload the plugin folder to `/wp-content/plugins/`
2. Activate the plugin via **Plugins → Installed Plugins**
3. Use the block editor to insert the **Page Not Found** block.

